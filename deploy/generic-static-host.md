# Generic static hosting

Upload the complete `dist/` directory to a conventional static host. Set `SITE_URL` to the public origin and `BASE_PATH` to `/` or the deployment subdirectory before building. Configure clean directory indexes and make the generated `404.html` the not-found page. No server adapter, function, database, or rewrite API is required.
