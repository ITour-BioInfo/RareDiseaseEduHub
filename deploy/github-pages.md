# GitHub Pages

Pages is not enabled. On GitHub Free, a Pages site requires this repository to be public; stop for explicit approval before changing visibility. If approved later, move and review the disabled template from `deploy/templates`, set `SITE_URL=https://ITour-BioInfo.github.io`, `BASE_PATH=/RareDiseaseEduHub/`, validate subdirectory links, and enable Pages with GitHub Actions as its source.
